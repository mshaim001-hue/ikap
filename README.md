# iKapitalist - Платформа для привлечения инвестиций

## Описание проекта

**iKapitalist** — это платформа для автоматизации процесса привлечения инвестиций для казахстанских компаний. Система собирает финансовую информацию от компаний через чат-бота с AI и автоматически анализирует документы для определения соответствия требованиям платформы.

## Основной функционал

### 1. Сбор заявок через AI-чат
- Чат-бот проводит диалог с клиентом
- Собирает основную информацию: сумма, срок, цель финансирования, БИН
- Принимает и категоризирует документы:
  - **Банковские выписки** (за 12 месяцев)
  - **Налоговая отчетность** (за текущий и предыдущий год)
  - **Финансовая отчетность** (баланс, ОПУ)
- Вся информация сохраняется в базе данных

### 2. Автоматический финансовый анализ

Система использует **3 специализированных AI-агента**:

#### Financial Analyst (GPT-5)
- Анализирует **только банковские выписки**
- Рассчитывает чистую выручку за 12 месяцев
- Группирует данные по месяцам
- Проверяет соответствие требованию: оборот ≥ 60 млн KZT
- Создает сводный отчет с графиками динамики

#### Tax Analyst (GPT-5)
- Анализирует налоговую отчетность
- Выявляет налоговые обязательства, задолженности, штрафы
- Проверяет покрытие за текущий и предыдущий год

#### Financial Statements Analyst (GPT-5)
- Анализирует баланс и ОПУ
- Рассчитывает ключевые метрики: выручка, прибыль, активы
- Оценивает динамику и риски

### 3. Управление заявками
- Просмотр списка всех заявок
- Детальная информация по каждой заявке
- Просмотр полной истории диалога
- Скачивание сгенерированных отчетов
- Просмотр загруженных файлов
- Удаление заявок

## Архитектура

### Frontend
- **React 18** + Vite
- Компоненты:
  - `Layout.jsx` - основная структура приложения
  - `Sidebar.jsx` - навигация между "Чат" и "Заявки"
  - `MainContent.jsx` - главный контент с чатом
  - `AgentsChat.jsx` - интерфейс чата с AI-ботами
  - `Applications.jsx` - список и детали заявок
  - `RightSidebar.jsx` - боковая панель
  - `Header.jsx` - шапка с логотипом
- Стилизация: CSS модули

### Backend
- **Express 5** сервер на Node.js
- **OpenAI Agents SDK** - для работы с AI-агентами
- **Multer** - загрузка файлов (PDF до 50MB)
- **SQLite/PostgreSQL** - база данных
- **CORS** - поддержка разных доменов

### База данных

**Таблицы:**
- `reports` - заявки и их статусы
  - `session_id` - уникальный ID сессии
  - `company_bin` - БИН компании
  - `amount`, `term`, `purpose` - параметры заявки
  - `name`, `email`, `phone` - контакты
  - `report_text` - основной отчет от финансового аналитика
  - `tax_report_text` - отчет от налогового аналитика
  - `fs_report_text` - отчет от аналитика фин. отчетности
  - `status`, `tax_status`, `fs_status` - статусы генерации
  - `files_count`, `files_data` - информация о файлах

- `messages` - история диалогов
  - `session_id`, `role`, `content`, `message_order`

- `files` - метаданные загруженных файлов
  - `session_id`, `file_id` (OpenAI), `original_name`, `category`

### API Endpoints

#### `/api/agents/run` (POST)
Основной endpoint для чата:
- Принимает текст и файлы
- Запускает Investment Agent для сбора данных
- При завершении сбора запускает 3 параллельных анализатора

#### `/api/reports` (GET)
Получение списка всех заявок

#### `/api/reports/:sessionId` (GET)
Детальная информация по заявке

#### `/api/reports/:sessionId/messages` (GET)
История диалога по заявке

#### `/api/files/:fileId/download` (GET)
Скачивание файла из OpenAI

#### `/api/reports/:sessionId` (DELETE)
Удаление заявки

#### `/api/reports/:sessionId/progress` (GET)
Прогресс по сбору документов

## Структура проекта

```
ikap/
├── src/
│   ├── components/          # React компоненты
│   │   ├── AgentsChat.jsx   # Чат-интерфейс
│   │   ├── Applications.jsx # Управление заявками
│   │   ├── Layout.jsx       # Обертка приложения
│   │   └── ...
│   ├── utils/
│   │   └── api.js          # API утилиты
│   ├── App.jsx             # Корневой компонент
│   └── main.jsx            # Точка входа
├── server/
│   ├── index.js            # Express сервер + AI логика
│   └── db.js               # Работа с БД
├── public/                 # Статические файлы
├── dist/                   # Собранный frontend
├── package.json
├── vite.config.js
├── render.yaml             # Конфигурация Render.com
└── deploy.yml              # GitHub Actions для деплоя
```

## Зависимости

### Frontend
- React 18
- Vite
- lucide-react (иконки)
- @openai/chatkit-react

### Backend
- Express 5
- OpenAI SDK 6.3
- @openai/agents 0.1.9
- Multer
- pg (PostgreSQL) / better-sqlite3
- CORS
- dotenv

## Переменные окружения

Создайте файл `.env`:

```env
OPENAI_API_KEY=sk-...
PORT=8787
NODE_ENV=development

# PostgreSQL (опционально, по умолчанию SQLite)
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Frontend URL для CORS
FRONTEND_URL=https://your-domain.com
```

## Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск frontend + backend
npm run dev

# Или отдельно
npm run dev:server  # Backend на порту 8787
npm run dev:vite    # Frontend на порту 5173
```

### Production

```bash
# Сборка frontend
npm run build

# Запуск сервера
npm start
```

## Деплой

### Render.com
Конфигурация в `render.yaml`. Сервис автоматически:
- Собирает frontend
- Запускает Express сервер
- Настраивает PostgreSQL БД

### GitHub Pages
```bash
npm run deploy
```

## Особенности работы AI-агентов

### Investment Agent (GPT-5-mini)
- Собирает данные пошагово
- НЕ анализирует файлы при загрузке
- Только принимает файлы
- Запускается с таймаутом 30 минут

### Financial Analyst (GPT-5)
- Использует Code Interpreter
- Анализирует ТОЛЬКО банковские выписки (категория 'statements')
- Исключает налоги и фин. отчетность
- Создает Excel отчеты и графики
- Таймаут 30 минут

### Tax Analyst (GPT-5)
- Анализирует только файлы категории 'taxes'
- Проверяет покрытие 2 лет

### Financial Statements Analyst (GPT-5)
- Анализирует только файлы категории 'financial'
- Проверяет покрытие 2 лет

### Категоризация файлов
Файлы автоматически категоризируются по имени:
- `taxes` - содержит "налог", "tax"
- `financial` - содержит "фин", "fin", "balance", "баланс"
- `statements` - все остальные PDF (банковские выписки)

## Лимиты и ограничения

- Размер файла: до 50 MB
- Формат: только PDF
- Максимум файлов за загрузку: 10
- Таймауты агентов: 30 минут
- Длина текстовых сообщений: максимум 200 символов

## Требования к данным

### Банковские выписки
- Минимум 12 месяцев
- Все валютные счета должны быть учтены
- Период должен покрывать последние 12 месяцев

### Налоговая отчетность
- Текущий год
- Предыдущий год

### Финансовая отчетность
- Баланс
- ОПУ
- Текущий год
- Предыдущий год

## Безопасность

- CORS настроен для конкретных доменов
- Файлы загружаются в OpenAI (не хранятся локально)
- Токены и пароли в `.env`
- Валидация форматов файлов

## Мониторинг и логирование

Сервер выводит подробные логи:
- `🤖` - запросы к AI агентам
- `📎` - загрузка файлов
- `💾` - работа с БД
- `✅` - успешные операции
- `❌` - ошибки
- `⏰` - таймауты

## Контакты

Вопросы по проекту: создатель репозитория

---

**Версия**: 1.0.0  
**Лицензия**: Владелец проекта

