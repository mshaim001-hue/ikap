# Обновление адреса бэкенда во фронтенде

## ✅ Что было сделано

1. **Обновлен `package.json`** - добавлен скрипт `build:prod` с правильным адресом
2. **Обновлен GitHub Actions workflow** - использует адрес по умолчанию если секрет не установлен
3. **CORS на бэкенде** - уже настроен для работы с `*.github.io` доменами

## 🚀 Быстрый деплой

### Вариант 1: Автоматический деплой через GitHub Actions

Просто запушьте изменения:

```bash
git add .
git commit -m "Update backend URL to Render"
git push
```

GitHub Actions автоматически использует адрес `https://ikap1-backend-latest.onrender.com` при сборке.

### Вариант 2: Локальный деплой

```bash
npm run deploy
```

Скрипт автоматически установит правильный адрес бэкенда.

## 📝 Настройка GitHub Secrets (Опционально)

Если хотите управлять адресом через секреты:

1. GitHub → Settings → Secrets and variables → Actions
2. Добавьте секрет:
   - **Name**: `VITE_API_BASE_URL`
   - **Value**: `https://ikap1-backend-latest.onrender.com`

## 🔍 Проверка

После деплоя проверьте в консоли браузера (F12):

```javascript
// Должны видеть запросы на:
https://ikap1-backend-latest.onrender.com/api/agents/run
https://ikap1-backend-latest.onrender.com/api/reports
// и т.д.
```

## 🌐 Адреса

- **Фронтенд**: `https://your-username.github.io/ikap/`
- **Бэкенд**: `https://ikap1-backend-latest.onrender.com`

## ✅ CORS

Бэкенд уже настроен на разрешение запросов с GitHub Pages:
- `https://*.github.io` - автоматически разрешено
- `https://*.githubpages.io` - автоматически разрешено

Дополнительная настройка не требуется!

