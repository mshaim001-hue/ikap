# Чеклист для проверки интеграции taxpdfto

## ✅ Проверка настройки taxpdfto на Render.com

Сервис развернут на: **https://ikap3-backend-latest.onrender.com**

### Необходимые переменные окружения в taxpdfto:

1. ✅ `OPENAI_API_KEY` - **ОБЯЗАТЕЛЬНО** для работы агента анализа
   - Без этого ключа анализ не будет работать
   - Проверьте в Render.com Dashboard → Environment Variables

2. ⚙️ `TAX_ANALYSIS_MODEL` - опционально (по умолчанию `gpt-4o`)
   - Можно установить другую модель, если нужно

### Проверка работы сервиса:

1. Откройте https://ikap3-backend-latest.onrender.com
2. Должна открыться страница загрузки PDF файлов
3. Попробуйте загрузить тестовый PDF файл

## ✅ Проверка настройки ikap

### Переменные окружения в ikap:

1. ✅ `TAX_PDF_SERVICE_URL` - уже настроен в `render.yaml`
   - Значение: `https://ikap3-backend-latest.onrender.com`
   - Проверьте в Render.com Dashboard → Environment Variables

### Как проверить интеграцию:

1. Загрузите налоговый PDF файл через интерфейс ikap
2. В логах ikap должны появиться сообщения:
   - `📡 Tax OCR (Render.com) включен: https://ikap3-backend-latest.onrender.com`
   - `✅ Получен готовый анализ от taxpdfto для "..."`
3. После завершения анализа откройте заявку
4. Нажмите кнопку "Аналитика по налогам"
5. Должен отобразиться готовый анализ от агента

## 🔧 Устранение неполадок

### Проблема: Анализ не выполняется

**Проверьте:**
1. Установлен ли `OPENAI_API_KEY` в taxpdfto?
2. Правильный ли URL в `TAX_PDF_SERVICE_URL`?
3. Работает ли сервис taxpdfto (откройте URL в браузере)?

**Логи:**
- В логах ikap ищите сообщения с префиксом `📡`, `✅`, `❌`
- В логах taxpdfto проверьте ошибки OpenAI API

### Проблема: Fallback на локальный агент

Если в логах видите:
```
🐍 Tax OCR: используется локальный Python (TAX_PDF_SERVICE_URL не задан)
```

**Решение:**
- Проверьте, что `TAX_PDF_SERVICE_URL` установлен в Render.com
- Перезапустите сервис ikap

### Проблема: Таймаут анализа

Если анализ занимает слишком много времени:
- Увеличьте таймаут в `taxPdfParser.js` (по умолчанию 20 минут для анализа)
- Проверьте размер PDF файлов (большие файлы требуют больше времени)

## 📝 Примечания

- Анализ выполняется **асинхронно** в taxpdfto
- Если анализ не получен от taxpdfto, ikap автоматически использует локальный агент (fallback)
- Готовый анализ сохраняется в поле `tax_report_text` в таблице `reports`
