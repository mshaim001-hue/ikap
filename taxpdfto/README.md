# Парсер налоговых деклараций

Веб-сервис для парсинга PDF файлов налоговых деклараций с использованием pdfplumber.

## Возможности

- 📄 Загрузка PDF файлов через веб-интерфейс
- 🔍 Автоматическое извлечение текста из PDF
- 📊 Отображение результатов в удобном HTML формате
- 💾 Возможность скачать результат в виде TXT файла
- 🎨 Современный и удобный интерфейс

## Установка

1. Убедитесь, что у вас установлен Python 3.7 или выше

2. Установите зависимости:
```bash
python3 -m pip install -r requirements.txt
```

## Запуск

Запустите приложение:
```bash
python3 app.py
```

Сервис будет доступен по адресу: http://localhost:5000

## Использование

1. Откройте браузер и перейдите на http://localhost:5000
2. Нажмите "Выберите файл" или перетащите PDF файл в область загрузки
3. Нажмите "Отправить на парсинг"
4. Просмотрите извлеченный текст на странице результатов
5. При необходимости скачайте результат в формате TXT

## Структура проекта

```
taxpdfto/
├── app.py              # Основное Flask приложение
├── requirements.txt    # Зависимости проекта
├── templates/          # HTML шаблоны
│   ├── index.html     # Главная страница
│   └── result.html    # Страница результатов
└── uploads/           # Временная папка для загрузок (создается автоматически)
```

## Технологии

- **Flask** - веб-фреймворк
- **pdfplumber** - библиотека для парсинга PDF
- **HTML/CSS/JavaScript** - фронтенд

## Примечания

- Максимальный размер файла: 16MB
- Поддерживаются только PDF файлы
- Загруженные файлы автоматически удаляются после обработки

