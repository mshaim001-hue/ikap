# Инструкция: Просмотр логов Supabase для диагностики разрывов соединения

## Почему происходит разрыв соединения?

Разрывы соединения (`error: {:shutdown, :db_termination}`) могут происходить по следующим причинам:

### 1. **Таймауты Supabase Pooler**
- Supabase использует пулер соединений (connection pooler) с ограничениями
- Неактивные соединения могут разрываться через определенное время (обычно 60-90 секунд)
- Соединения могут разрываться при отсутствии активности

### 2. **Превышение лимита подключений**
- Бесплатный план Supabase: **60 одновременных подключений**
- Pro план: зависит от размера инстанса
- Если лимит превышен, новые подключения отклоняются

### 3. **Сетевые проблемы**
- Нестабильное соединение между сервером и Supabase
- Таймауты на сетевом уровне
- Проблемы с SSL/TLS

### 4. **Превышение лимитов ресурсов**
- Превышение лимита CPU/RAM на плане
- Долгие запросы (> 60 секунд) могут быть разорваны

## Как посмотреть логи в Supabase?

### Шаг 1: Войдите в Supabase Dashboard

1. Перейдите на [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. Войдите в свой аккаунт
3. Выберите проект, в котором происходят проблемы

### Шаг 2: Откройте раздел "Logs"

1. В левом меню найдите раздел **"Logs"** или **"Логи"**
2. Или перейдите напрямую: `https://supabase.com/dashboard/project/[YOUR_PROJECT_ID]/logs`

### ⚠️ ВАЖНО: Ошибки pooler'а могут быть не в основных логах!

Если вы видите в логах только обычные подключения (`connection authorized`, `checkpoint complete`) и нет ошибок `db_termination`, это может означать:

1. **Ошибки логируются на уровне pooler'а**, а не основного PostgreSQL
2. **Временной диапазон не охватывает момент ошибки** — попробуйте расширить диапазон
3. **Ошибки в другом источнике логов** — проверьте фильтры:
   - В фильтре "Source" выберите **"Connection Pooler"** или **"Pooler"**
   - Или ищите логи с вашим хостом: `aws-1-eu-west-1.pooler.supabase.com`

### Шаг 2.1: Проверьте фильтры логов

При поиске ошибок разрыва соединения:

1. **Source (Источник):** Выберите **"Connection Pooler"** или **"Pooler"** вместо "Primary database"
2. **Severity (Уровень):** Выберите **"Error"** или **"FATAL"**
3. **Time Range (Временной диапазон):** Расширьте до периода, когда происходили ошибки
4. **Search (Поиск):** Введите: `db_termination`, `shutdown`, `XX000`, или `terminated`

### Шаг 3: Выберите категорию логов

Supabase предоставляет несколько категорий логов:

#### 📊 **Database Logs (Логи базы данных)**
- **Где:** `Logs` → `Database` или `Database Logs`
- **Что показывают:**
  - Ошибки подключения (`FATAL: Max client connections reached`)
  - Разрывы соединения (`db_termination`)
  - Медленные запросы
  - Ошибки SQL запросов

#### 🔍 **API Logs (Логи API)**
- **Где:** `Logs` → `API`
- **Что показывают:**
  - Запросы к API
  - Ошибки API
  - Статус коды

#### 🔐 **Auth Logs (Логи аутентификации)**
- **Где:** `Logs` → `Auth`
- **Что показывают:**
  - Ошибки входа
  - Проблемы с токенами

#### 📈 **Postgres Logs (Подробные логи PostgreSQL)**
- **Где:** `Logs` → `Postgres Logs` или `Database` → `Logs`
- **Что показывают:**
  - Детальные логи PostgreSQL
  - Ошибки `XX000` (разрыв соединения)
  - Предупреждения о таймаутах

### Шаг 4: Фильтрация логов

Для поиска ошибок разрыва соединения используйте фильтры:

1. **По типу ошибки:**
   - Ищите: `db_termination`, `shutdown`, `XX000`, `FATAL`
   
2. **По времени:**
   - Выберите временной диапазон, когда происходили ошибки
   
3. **По серверу:**
   - `aws-1-eu-west-1.pooler.supabase.com` (ваш сервер пулера)

### Примеры ошибок, которые нужно искать:

```
error: {:shutdown, :db_termination}
FATAL: Max client connections reached
FATAL: remaining connection slots are reserved
error code XX000
connection terminated
connection closed
unexpected EOF on client connection
```

### 🔍 Где именно искать ошибки разрыва соединения:

#### Вариант 1: Логи Connection Pooler
1. В фильтре **"Source"** выберите **"Connection Pooler"** или **"Pooler"**
2. Ищите ошибки в периоды, когда происходили разрывы

#### Вариант 2: Фильтр по хостнейму
1. В поиске (Search) введите: `pooler.supabase.com`
2. Это покажет все логи, связанные с пулером соединений

#### Вариант 3: Расширенный временной диапазон
1. Если ошибка была в 12:53 PM (как в вашем терминале)
2. Установите диапазон: **"Last 24 hours"** или **"Custom range"**
3. Выберите период вокруг 12:53 PM

#### Вариант 4: Проверка через SQL (если логи не помогают)
В Supabase SQL Editor выполните:

```sql
-- Проверить недавние ошибки в PostgreSQL логах (если доступно)
SELECT * FROM pg_stat_database WHERE datname = 'postgres';

-- Проверить активные подключения и их состояние
SELECT 
    pid,
    usename,
    application_name,
    client_addr,
    state,
    query_start,
    state_change,
    wait_event_type,
    wait_event
FROM pg_stat_activity 
WHERE datname = 'postgres' 
  AND pid != pg_backend_pid()
ORDER BY state_change DESC;
```

## Дополнительная диагностика

### Проверка количества активных подключений

В Supabase SQL Editor выполните:

```sql
-- Проверить активные подключения
SELECT 
    datname,
    count(*) as connections,
    state,
    usename
FROM pg_stat_activity
WHERE datname = 'postgres'
GROUP BY datname, state, usename
ORDER BY connections DESC;

-- Проверить лимит подключений
SHOW max_connections;
```

### Проверка пула соединений

1. В Supabase Dashboard: `Settings` → `Database`
2. Найдите раздел **"Connection Pooling"**
3. Проверьте:
   - **Mode:** Transaction / Session
   - **Pool Size:** количество доступных соединений
   - **Max Connections:** максимальное количество

### Рекомендации по улучшению стабильности

1. **Используйте Transaction Pooling** (режим транзакций) вместо Session
2. **Уменьшите размер пула** на клиенте (сейчас `max: 5`)
3. **Увеличьте `idleTimeoutMillis`** (уже установлено 60 секунд)
4. **Используйте переподключение** при ошибках (уже реализовано)

## Где еще посмотреть информацию?

### 1. Supabase Dashboard → Settings → Database
- Информация о подключении
- Настройки пула соединений
- Лимиты ресурсов

### 2. Supabase Dashboard → Project Settings
- Лимиты плана
- Использование ресурсов
- Статистика подключений

### 3. Метрики (Metrics)
- **Где:** `Dashboard` → `Metrics` или `Usage`
- **Что показывают:**
  - Количество запросов
  - Использование CPU/RAM
  - Количество активных подключений
  - Таймауты запросов

## Быстрые ссылки

- [Supabase Dashboard](https://supabase.com/dashboard)
- [Supabase Docs: Connection Pooling](https://supabase.com/docs/guides/database/connecting-to-postgres#connection-pooler)
- [Supabase Docs: Database Logs](https://supabase.com/docs/guides/platform/logs)

