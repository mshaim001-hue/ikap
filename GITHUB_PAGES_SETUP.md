# Настройка фронтенда для GitHub Pages

## Обновление адреса бэкенда

Адрес бэкенда обновлен на: **https://ikap1-backend-latest.onrender.com**

## Вариант 1: Через GitHub Secrets (Рекомендуется)

### 1. Добавьте секрет в GitHub

1. Зайдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажмите **New repository secret**
4. Добавьте:
   - **Name**: `VITE_API_BASE_URL`
   - **Value**: `https://ikap1-backend-latest.onrender.com`
5. Сохраните

### 2. Деплой

GitHub Actions автоматически использует этот секрет при сборке:
```bash
git add .
git commit -m "Update backend URL"
git push
```

## Вариант 2: Локальный деплой

Если деплоите локально через `npm run deploy`:

```bash
# Используйте обновленный скрипт
npm run deploy
```

Скрипт автоматически установит правильный адрес бэкенда.

## Вариант 3: Ручная установка переменной

При сборке вручную:

```bash
VITE_API_BASE_URL=https://ikap1-backend-latest.onrender.com npm run build
gh-pages -d dist
```

## Проверка CORS на бэкенде

Убедитесь что на Render.com в переменных окружения установлено:

```env
FRONTEND_URL=https://your-username.github.io
```

Или если репозиторий называется `ikap`:
```env
FRONTEND_URL=https://your-username.github.io/ikap
```

Бэкенд уже настроен на разрешение запросов с `*.github.io` доменов, так что это должно работать автоматически.

## Проверка работы

После деплоя:

1. Откройте ваш сайт на GitHub Pages
2. Откройте консоль браузера (F12)
3. Проверьте что запросы идут на правильный адрес:
   ```
   🌐 API Call: https://ikap1-backend-latest.onrender.com/api/...
   ```

## Структура URL

- **Фронтенд**: `https://your-username.github.io/ikap/` (или без `/ikap/` если username.github.io)
- **Бэкенд**: `https://ikap1-backend-latest.onrender.com`

## Обновление адреса в будущем

Если адрес бэкенда изменится:

1. Обновите GitHub Secret `VITE_API_BASE_URL`
2. Или обновите скрипт `build:prod` в `package.json`
3. Пересоберите и задеплойте

